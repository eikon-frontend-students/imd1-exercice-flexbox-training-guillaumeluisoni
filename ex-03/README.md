# Ex-03 — Direction

## Objectif
Passer de `row` à `column`, puis finir en `column-reverse`.

## Règles
- Modifier uniquement `style.css`.
- Utiliser uniquement Flexbox.
- `styles/base.css` est intouchable.

## Contraintes
- HTML inchangé.
- Pas de CSS Grid.
- Pas de `position` pour la mise en page.

## Auto-validation
- [ ] Les items sont empilés verticalement.
- [ ] L'ordre final est inverse (4 en haut, 1 en bas).
- [ ] Je n'ai pas touché au HTML.
